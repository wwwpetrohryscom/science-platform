NEXT_PUBLIC_GA_MEASUREMENT_ID=G-CT4JPTP4DX

# Set to "true" only once app/api/newsletter/route.ts actually hands off to a
# provider. Until then the subscribe form stays hidden, because the copy
# promises delivery the site cannot make and the route stores nothing.
NEXT_PUBLIC_NEWSLETTER_ENABLED=false
